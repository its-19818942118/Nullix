[tool.poetry]
name = "nullix"
version = "0.1.0"
description = ""
authors = ["Batuhan Edgüer"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12.0"
rich = "^13.8.0"
click = "^8.1.7"
distro = "^1.9.0"
pyyaml = "^6.0.2"
pydantic = "^2.8.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
nullix = "nullix.cli:cli"
